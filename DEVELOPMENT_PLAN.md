خطة تطوير مشروع "Crystal Feather" (موجز تنفيذي وبنود عمل)

الهدف
- تحسين الأمان، الاعتمادية، وقابلية التطوير لمشروع مكتبة رقمية مبني على Node.js + Express + Supabase.

ملخص سريع للحالة الحالية
- خادم Express في `server.js`، يعتمد Supabase للتخزين والـCRUD.
- واجهة ثابتة في مجلد `public/`، ورفع ملفات إلى `uploads/`.
- آليات توثيق: مزيج بين جلسات وJWT.
- نقاط ضعف مبدئية: متغير `JWT_SECRET` غير معرف محلياً، عدم تقييد CORS، رفع ملفات بدون تحقق، CSP معطّل، عدم وجود `.env.example` وREADME.

Milestone A — إعداد بيئة واصلاحات حرِجة (أولوية عالية)
- A1: تعريف وتوحيد متغير `JWT_SECRET`
  - الملف: `server.js` — إضافة `const JWT_SECRET = process.env.JWT_SECRET || 'jwt-secret-999';`
  - وقت تقديري: 1 ساعة
- A2: إنشاء `.env.example` وتحديث `README.md` لتعليمات التشغيل
  - متغيرات مقترحة: `SUPABASE_URL`, `SUPABASE_KEY`, `JWT_SECRET`, `SESSION_SECRET`, `PORT`
  - وقت تقديري: 1 ساعة
- A3: تفعيل `express-rate-limit` لمسارات المصادقة ورفع الملفات
  - وقت تقديري: 2 ساعة
- A4: تقييد CORS إلى Origins موثوقة بدلاً من السماح الشامل
  - وقت تقديري: 1 ساعة

Milestone B — أمان رفع الملفات والتحقق (أولوية متوسطة)
- B1: إضافة فلاتر `multer` للتحقق من النوع (PDF فقط للمخطوطات، صور فقط للأغلفة)
  - تحقق من امتدادات MIME وحجم الملف (مثلاً 50MB للـPDF، 5MB للصور)
  - وقت تقديري: 3 ساعات
- B2: تنقية أسماء الملفات وتخزينها بطريقة آمنة أو الانتقال إلى تخزين سحابي (Supabase storage أو S3)
  - وقت تقديري: 3 ساعات

Milestone C — بنية، معالجة أخطاء، والتحقق من المدخلات (أولوية متوسطة)
- C1: فصل الطبقات: `routes/`, `controllers/`, `services/` بدل ملف واحد عملاق
  - وقت تقديري: 1-2 أيام (بناء تدريجي)
- C2: إضافة middleware مركزي لمعالجة الأخطاء (`errorHandler`) واستخدام `try/catch` في الـasync handlers
  - وقت تقديري: 4 ساعات
- C3: إدخال مكتبة تحقق (مثلاً `joi` أو استخدام `validator`) لفلترة المدخلات
  - وقت تقديري: 4-6 ساعات

Milestone D — اختبارات وCI (أولوية متوسطة)
- D1: إعداد `jest` + `supertest` لاختبارات تكاملية لمسارات الأساس (auth, books, upload)
  - وقت تقديري: 1-2 أيام
- D2: إعداد GitHub Actions لتشغيل lint + tests على PRs
  - وقت تقديري: 3-4 ساعات

Milestone E — تحسينات إنتاجية وتجربة المستخدم (أولوية منخفضة)
- E1: نقل التخزين إلى Supabase Storage أو S3 لسهولة التوسع
  - وقت تقديري: 1 يوم
- E2: إضافة لوحة إدارة أفضل، تقارير إيرادات، وواجهة إدارة الكتب المعلقة
  - وقت تقديري: 2-4 أيام
- E3: ترحيل المشروع إلى TypeScript (اختياري، لتحسين الصيانة)
  - وقت تقديري: يعتمد على النطاق، 1-2 أسابيع تقريبي

مهام تشغيلية فورية أنصح بتنفيذها الآن (خلال نفس الجلسة)
- إصلاح تعريف `JWT_SECRET` في `server.js`.
- إضافة `.env.example` و`README.md` صغير يوضّح كيفية إعداد البيئة وتشغيل المشروع.
- تفعيل `express-rate-limit` وقيود CORS الأساسية.

مقترحات للمرحلة القادمة
- إذا ترغب، أبدأ الآن بتنفيذ التعديلات الحرِجة (A1-A4) مع فتح commit واحد لكل Milestone.
- بعد الموافقة، سأضيف أيضاً اختبارات أساسية ومسار CI.

ملف الخطة هذا مُنشأ تلقائياً. للتنفيذ الفعلي للتغييرات، أخبرني أي Milestone تريدني أن أبدأ به الآن.